# Local dummy secret fixture for Firebase Functions emulator.
# Copy to functions/.secret.local for local offline emulator execution:
# cp .secret.local.example .secret.local
# This avoids Secret Manager cloud lookups and prevents any real secret exposure.
GEMINI_API_KEY=dummy-local-synthetic-key
